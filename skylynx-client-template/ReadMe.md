# skylynx-client-template
> This folder contains a base template for all POC projects moving forward. 
> //TODO: 1 Use the patterens in place in the src folder Update the current folder Remove anything that makes it hard to use as a template. Remove all Auth this will not use Auth and if we use Auth it will be a mockup. Convert Files under source to be used has a template make sure Auth is removed but we want to follow core patterens keep the bootstrap and plash screen but the image will be changed for each POC make this easier to do as a 
>
> //TODO: 2. remove the Portal concept each POC will be it's own Portal

> //TODO: 3. update this readme file in order to use the template created under the src folder